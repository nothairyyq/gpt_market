<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.priska.infrastructure.persistent.dao.IRaffleActivityAccountDao">

    <resultMap id="dataMap" type="com.priska.infrastructure.persistent.po.RaffleActivityAccount">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="activity_id" property="activityId"/>
        <result column="total_count" property="totalCount"/>
        <result column="total_count_surplus" property="totalCountSurplus"/>
        <result column="day_count" property="dayCount"/>
        <result column="day_count_surplus" property="dayCountSurplus"/>
        <result column="month_count" property="monthCount"/>
        <result column="month_count_surplus" property="monthCountSurplus"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <update id="updateAccountQuota" parameterType="com.priska.infrastructure.persistent.po.RaffleActivityAccount">
        update raffle_activity_account
        set
            total_count = total_count + #{totalCount},
            total_count_surplus = total_count_surplus + #{total_count_surplus},
            day_count = day_count + #{day_count},
            day_count_surplus = day_count_surplus + #{day_count_surplus},
            month_count = month_count + #{month_count},
            month_count_surplus = month_count_surplus + #{month_count_surplus},
            update_time = now()
        where user_id = #{userId} and activity_id = #{activityId}
    </update>
</mapper>
